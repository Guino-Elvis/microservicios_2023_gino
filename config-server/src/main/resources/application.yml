server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Guino-Elvis/microservicios_2023_gino.git
          search-paths: config-data
          username: Guino-Elvis
          password: gino123456123
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
